version: "3.8"

services:
  mariadb:
    image: mariadb
    restart: always
    environment:
      - "MYSQL_ROOT_PASSWORD=example"
    ports: 
      - 3306:3306

  adminer:
    image: adminer
    restart: always
    ports:
      - 8080:8080
